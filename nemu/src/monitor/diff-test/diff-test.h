#ifndef __DIFF_TEST_H__
#define __DIFF_TEST_H__

//#define DIFFTEST_REG_SIZE (sizeof(uint32_t) * 9) // GRPs + EIP
//#define DIFFTEST_REG_SIZE (sizeof(uint32_t) * 10) // GRPs + EIP + eflags
#define DIFFTEST_REG_SIZE (sizeof(CPU_state))

#endif
